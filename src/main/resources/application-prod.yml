server:
  port: 8080

spring:
  application:
    name: lead-crm-backend-prod

  datasource:
    url: jdbc:mysql://43.155.154.87:3306/lead_crm?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: wangzq100416
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: ${JWT_SECRET:lead-crm-prod-jwt-secret-key-very-long-string-for-security}
  expiration: 86400000

logging:
  level:
    com.leadcrm: INFO
    org.springframework.security: WARN
